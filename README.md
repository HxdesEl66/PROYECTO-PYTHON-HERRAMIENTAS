# Sistema de control de herramientas del vecindario

## Requisitos
- Python 3.10 o superior
- (Opcional) entorno virtual

## Instalación
1. Clona o copia esta carpeta `vecindario_herramientas/`.
2. (Opcional) Crea y activa un entorno virtual:
   ```bash
   python -m venv .venv
   # Linux/Mac
   source .venv/bin/activate
   # Windows
   .venv\Scripts\activate

